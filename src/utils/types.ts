export type Local = {
    cep?: string,
    logradouro?: string,
    complemento?: string,
    bairro?: string,
    localidade?: string,
    uf?: string,
    unidade?: string,
    ibge?: string,
    gia?: string
}

export type FormatFields = {label: string, value: string};